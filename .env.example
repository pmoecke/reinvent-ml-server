#######################################
# 🌱 General App Configuration
#######################################
APP_ENV=dev
LOG_LEVEL=INFO
WORKERS=1

#######################################
# 🗄️ Database (Postgres + pgvector)
#######################################
# Example for local setup
DB_NAME=reinvent_postgres
DB_HOST_EXTERNAL=localhost
DB_HOST_INTERNAL=postgres
DB_USER=user
DB_PASSWORD=password
DB_PORT=5432
PG_SEARCH_PATH=ai,public

#######################################
# 📦 MinIO (for later integration)
#######################################
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_SECURE=False

#######################################
# 🧠 Embeddings
#######################################
EMBED_PROVIDER=huggingface
EMBED_MODEL_NAME=intfloat/multilingual-e5-small
EMBED_DIM=384
EMBED_DEVICE=auto
EMBED_BATCH_SIZE=128
HF_HOME=.cache/huggingface
HF_HUB_OFFLINE=False

#######################################
# 💬 LLM (SwissAI / Apertus)
#######################################
LLM_API_BASE=https://api.swissai.cscs.ch/v1
# 👉 Replace with your actual SwissAI API key
LLM_API_KEY=sk-rc-************
LLM_MODEL=swissai/Apertus-70B-Instruct
# LLM_TIMEOUT=120  # Uncomment if you add this field in Settings

#######################################
# 🧱 Table naming (pgvector)
#######################################
PG_SCHEMA=ai
PG_TABLE=llamaindex

#######################################
# Authorization Tokens for basic security
#######################################
ML_API_TOKEN=some-very-secure-key-to-secure-the-ml-service-endpoints
DB_API_TOKEN=some-very-secure-key-to-secure-the-main-backend-endpoints